const { add, subtract, multiply, divide } = require('../src/calculator');

// ==========================================
// 덧셈 테스트
// ==========================================
describe('add()', () => {
  test('양수 두 개를 더한다', () => {
    expect(add(2, 3)).toBe(5);
  });

  test('음수와 양수를 더한다', () => {
    expect(add(-1, 5)).toBe(4);
  });

  test('소수를 더한다', () => {
    expect(add(0.1, 0.2)).toBeCloseTo(0.3);
  });

  test('숫자가 아닌 입력은 에러를 던진다', () => {
    expect(() => add('a', 1)).toThrow(TypeError);
  });
});

// ==========================================
// 뺄셈 테스트
// ==========================================
describe('subtract()', () => {
  test('큰 수에서 작은 수를 뺀다', () => {
    expect(subtract(10, 3)).toBe(7);
  });

  test('결과가 음수가 되는 경우', () => {
    expect(subtract(3, 10)).toBe(-7);
  });
});

// ==========================================
// 곱셈 테스트
// ==========================================
describe('multiply()', () => {
  test('두 양수를 곱한다', () => {
    expect(multiply(4, 5)).toBe(20);
  });

  test('0을 곱하면 0이 된다', () => {
    expect(multiply(999, 0)).toBe(0);
  });

  test('음수끼리 곱하면 양수가 된다', () => {
    expect(multiply(-3, -4)).toBe(12);
  });
});

// ==========================================
// 나눗셈 테스트
// ==========================================
describe('divide()', () => {
  test('나눗셈 결과를 반환한다', () => {
    expect(divide(10, 2)).toBe(5);
  });

  test('0으로 나누면 에러를 던진다', () => {
    expect(() => divide(10, 0)).toThrow('0으로 나눌 수 없습니다.');
  });

  test('소수 결과를 처리한다', () => {
    expect(divide(7, 2)).toBe(3.5);
  });
});

// ==========================================
// [실습] 아래 테스트를 통과시켜 보세요!
// 현재 버그가 있는 함수를 고쳐야 합니다.
// ==========================================
describe('[실습] 버그 수정 미션', () => {
  test('add(0, 0)은 0이어야 한다', () => {
    expect(add(0, 0)).toBe(0);
  });

  test('subtract(5, 5)는 0이어야 한다', () => {
    expect(subtract(5, 5)).toBe(0);
  });
});
